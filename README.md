# local-remote

create `zenremote` union of TDs \
create `zenstorage` using access keys \
`wget -O - https://raw.githubusercontent.com/CoffeeKnyte/local-remote/main/zenn.sh | sudo bash` \
alias `zenstall`
